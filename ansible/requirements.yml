---
# Ansible Galaxy requirements for the Matrix playbook.
# Install with:  ansible-galaxy collection install -r requirements.yml

collections:
  - name: community.general    # ufw, docker modules
    version: ">=8.0.0"
  - name: ansible.posix        # sysctl, mount helpers
    version: ">=1.5.0"
